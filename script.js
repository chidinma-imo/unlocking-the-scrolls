const scroll=document.getElementById('scroll');window.addEventListener('scroll',()=>{if(scroll){const y=window.scrollY;scroll.style.transform=`rotate(${-7+y*.012}deg) perspective(900px) rotateY(${-15+y*.03}deg)`}});document.querySelectorAll('.scroll-grid article').forEach((card,i)=>card.addEventListener('click',()=>{card.querySelector('span').textContent=card.classList.contains('open')?'CLOSE ↑':'OPEN →'}));
